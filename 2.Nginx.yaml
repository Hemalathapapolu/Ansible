- name: install and run ngnix
  host: web
  become: yes #becoming root user
  task:
    - name: install nginx
      ansible.builtin.dnf:
        name: nginx
        state: latest
    - name: start nginx
      ansible.builtin.service:
        name: nginx
        state: started
        enabled: yes
